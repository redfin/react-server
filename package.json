{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test && cd integration-tests && npm install && npm test"
  },
  "devDependencies": {
    "lerna": "v2.0.0-beta.5"
  }
}
